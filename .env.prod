# Production Environment Configuration
# Deploy this file to production server (keep secrets secure!)

# ============ DATABASE ============
# PostgreSQL Connection String
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://solvestack_user:CHANGE_THIS_PASSWORD@localhost:5432/solvestack

# ============ AUTHENTICATION ============
# Generate secure secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_THIS_TO_SECURE_RANDOM_STRING

# ============ REDDIT API ============
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
REDDIT_USER_AGENT=SolveStack/1.0 (Production)

# ============ GITHUB API ============
GITHUB_TOKEN=your_github_personal_access_token

# ============ FIREBASE (Phase 5) ============
FIREBASE_CREDENTIALS_PATH=/app/firebase-service-account.json

# ============ PRODUCTION SETTINGS ============
ENVIRONMENT=production
LOG_LEVEL=INFO
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
