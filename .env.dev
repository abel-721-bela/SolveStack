# Development Environment Configuration
# Use this file for local development with SQLite

# Database URL (leave blank to use SQLite)
# For PostgreSQL local testing: DATABASE_URL=postgresql://username:password@localhost:5432/solvestack
DATABASE_URL=

# JWT Secret (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
SECRET_KEY=your-secret-key-here-change-in-production

# Reddit API Credentials (optional for development)
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
REDDIT_USER_AGENT=SolveStack/1.0

# GitHub API Token (optional for development)
GITHUB_TOKEN=your_github_personal_access_token

# Firebase Service Account (optional - for Phase 5)
FIREBASE_CREDENTIALS_PATH=path/to/firebase-service-account.json
